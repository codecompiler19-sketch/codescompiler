---
title: "JS Dates"
description: "JavaScript Date objects let us work with dates."
category: "javascript"
order: 17
---

By default, JavaScript will use the browser's time zone and display a date as a full text string.

### Creating Date Objects
Date objects are created with the `new Date()` constructor.
There are 4 ways to create a new date object:
*   `new Date()`
*   `new Date(year, month, day, hours, minutes, seconds, milliseconds)`
*   `new Date(milliseconds)`
*   `new Date(date string)`

<Editor 
    initialHtml={`<h2>JavaScript new Date()</h2>\n<p id="demo"></p>`}
    initialJs={`const d = new Date();\ndocument.getElementById("demo").innerHTML = d;`}
/>

### new Date(year, month, ...)
`new Date(year, month, ...)` creates a new date object with a specified date and time.
7 numbers specify year, month, day, hour, minute, second, and millisecond (in that order).

> **Note:** JavaScript counts months from 0 to 11. January is 0. December is 11.

<Editor 
    initialHtml={`<h2>JavaScript new Date(year, month)</h2>\n<p id="demo"></p>`}
    initialJs={`const d = new Date(2018, 11, 24, 10, 33, 30, 0);\ndocument.getElementById("demo").innerHTML = d;`}
/>

### Date Get Methods
You can easily extract the current year, month, or day using Date methods.

<Editor 
    initialHtml={`<h2>JavaScript Date Get Methods</h2>\n<p id="y"></p>\n<p id="m"></p>`}
    initialJs={`const d = new Date();\ndocument.getElementById("y").innerHTML = "Year: " + d.getFullYear();\ndocument.getElementById("m").innerHTML = "Month: " + d.getMonth();`}
/>
