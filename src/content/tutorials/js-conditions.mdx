---
title: "JS Conditions"
description: "Conditional statements are used to perform different actions based on different conditions."
category: "javascript"
order: 22
---

Conditional statements are used to perform different actions based on different conditions.

### Conditional Statements
Very often when you write code, you want to perform different actions for different decisions.
You can use conditional statements in your code to do this.

In JavaScript we have the following conditional statements:
*   Use `if` to specify a block of code to be executed, if a specified condition is true
*   Use `else` to specify a block of code to be executed, if the same condition is false
*   Use `else if` to specify a new condition to test, if the first condition is false
*   Use `switch` to specify many alternative blocks of code to be executed

### The if Statement
Use the `if` statement to specify a block of JavaScript code to be executed if a condition is true.

<Editor 
    initialHtml={`<h2>The if Statement</h2>\n<p>Time based greeting:</p>\n<p id="demo"></p>`}
    initialJs={`const time = new Date().getHours();\nlet greeting;\n\nif (time < 18) {\n  greeting = "Good day!";\n}\n\ndocument.getElementById("demo").innerHTML = greeting;`}
/>

### The else and else if Statement
Use the `else if` statement to specify a new condition if the first condition is false. Use the `else` statement to catch everything else.

<Editor 
    initialHtml={`<h2>The else if Statement</h2>\n<p id="demo"></p>`}
    initialJs={`const time = new Date().getHours();\nlet greeting;\n\nif (time < 10) {\n  greeting = "Good morning";\n} else if (time < 20) {\n  greeting = "Good day";\n} else {\n  greeting = "Good evening";\n}\n\ndocument.getElementById("demo").innerHTML = greeting;`}
/>
