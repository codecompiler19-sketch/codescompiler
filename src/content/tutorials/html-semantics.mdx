---
title: "HTML Semantics"
description: "Semantic elements clearly describe their meaning in a human- and machine-readable way."
category: "html"
order: 21
---

Semantic elements clearly describe their meaning in a human- and machine-readable way.

### What are Semantic Elements?
A semantic element clearly describes its meaning to both the browser and the developer.

Examples of **non-semantic** elements: `<div>` and `<span>` - Tells nothing about its content.
Examples of **semantic** elements: `<form>`, `<table>`, and `<article>` - Clearly defines its content.

### Semantic Elements in HTML5
Many web sites contain HTML code like: `<div id="nav">` `<div class="header">` `<div id="footer">` to indicate navigation, header, and footer.
HTML5 offers new semantic elements to define different parts of a web page:
*   `<article>`
*   `<aside>`
*   `<details>`
*   `<figcaption>`
*   `<figure>`
*   `<footer>`
*   `<header>`
*   `<main>`
*   `<mark>`
*   `<nav>`
*   `<section>`
*   `<summary>`
*   `<time>`

<Editor 
    initialHtml={`<h2>Semantic HTML Layout</h2>\n<header style="background-color: #f1f1f1; padding: 20px; text-align: center;">\n  <h1>My Website Header</h1>\n</header>\n\n<section style="padding: 20px;">\n  <h2>About Us</h2>\n  <p>We are learning semantic HTML which helps screen readers read pages better.</p>\n</section>\n\n<footer style="background-color: #333; color: white; padding: 10px; text-align: center;">\n  <p>Company Footer</p>\n</footer>`}
/>
