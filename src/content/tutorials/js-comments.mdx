---
title: "JS Comments"
description: "JavaScript comments can be used to explain code and make it more readable."
category: "javascript"
order: 5
---

JavaScript comments are ignored by the browser — they exist only to help **you and other developers** understand the code.

There are two types of comments:

| Type | Syntax | Use |
| :--- | :--- | :--- |
| Single-line | `// your comment` | Short note on one line |
| Multi-line | `/* your comment */` | Longer explanations spanning multiple lines |

### Try It — Comments in Action

<Editor 
    initialHtml={`<h2>JavaScript Comments</h2>\n<p id="result1"></p>\n<p id="result2"></p>\n<p id="result3"></p>`}
    initialCss={`body { font-family: Arial, sans-serif; padding: 16px; }\nh2 { color: #04AA6D; }\np { background: #f4f4f4; padding: 8px 12px; border-radius: 4px; margin: 6px 0; }`}
    initialJs={`// Single-line comment: Change heading content\ndocument.getElementById("result1").innerHTML = "✅ Single-line comment: This line runs normally.";\n\n/*\n  Multi-line comment:\n  This block explains what the code below does.\n  It can span as many lines as needed.\n*/\nlet x = 5;\nlet y = 10;\ndocument.getElementById("result2").innerHTML = "✅ Multi-line comment above this code. x + y = " + (x + y);\n\n// To DISABLE code, comment it out:\n// document.getElementById("result3").innerHTML = "❌ This line is commented OUT — it won't run.";\ndocument.getElementById("result3").innerHTML = "✅ This line IS active and runs.";`}
/>

### When to Use Comments

- Explain **why** you wrote the code (not just what it does)
- Temporarily **disable** a line of code while debugging
- Add **section headers** to organise long scripts

> **Tip:** Use `Ctrl + /` in most code editors to quickly toggle comments on the selected line(s).
