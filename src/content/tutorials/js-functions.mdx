---
title: "JS Functions"
description: "A JavaScript function is a block of code designed to perform a particular task."
category: "javascript"
order: 9
---

A function is defined once and can be **called (invoked) as many times as needed**. This prevents code repetition and keeps your programs organised.

**Syntax:**
```js
function functionName(parameter1, parameter2) {
  // code to run
  return result;
}
```

### Try It — Functions in Action

<Editor 
    initialHtml={`<h2>JavaScript Functions</h2>\n\n<p id="multiply"></p>\n<p id="greet"></p>\n<p id="area"></p>\n\n<hr style="margin:16px 0">\n<button onclick="changeGreeting('Alice')">Greet Alice</button>\n<button onclick="changeGreeting('Bob')">Greet Bob</button>\n<p id="dynamic"></p>`}
    initialCss={`body { font-family: Arial, sans-serif; padding: 16px; }\nh2 { color: #04AA6D; }\np { background: #f4f4f4; padding: 8px 12px; border-radius: 4px; margin: 6px 0; font-family: monospace; }\nbutton {\n  background: #04AA6D; color: white; border: none;\n  padding: 8px 16px; border-radius: 5px; cursor: pointer; margin: 4px;\n}\nbutton:hover { background: #038a58; }`}
    initialJs={`// Function with parameters and return value\nfunction multiply(a, b) {\n  return a * b;\n}\ndocument.getElementById("multiply").innerHTML =\n  "multiply(4, 3) = " + multiply(4, 3);\n\n// Function that builds a greeting string\nfunction greet(name) {\n  return "Hello, " + name + "! Welcome to CodesCompiler.";\n}\ndocument.getElementById("greet").innerHTML = greet("World");\n\n// Function that calculates a rectangle area\nfunction rectArea(width, height) {\n  return width * height;\n}\ndocument.getElementById("area").innerHTML =\n  "Area of 8 x 5 rectangle = " + rectArea(8, 5) + " sq units";\n\n// Function called by button clicks\nfunction changeGreeting(name) {\n  document.getElementById("dynamic").innerHTML =\n    "👋 " + greet(name);\n}`}
/>

### Key Concepts

| Concept | Description | Example |
| :--- | :--- | :--- |
| Definition | Declare the function | `function add(a, b) { ... }` |
| Parameter | Input the function receives | `a` and `b` above |
| Return | Value the function sends back | `return a + b;` |
| Invocation | Calling the function | `add(3, 5)` → `8` |

> **Tip:** Functions that return a value can be used directly in expressions: `let result = multiply(4, 3) + 10;`
