---
title: "HTML Emojis"
description: "Emojis are characters from the UTF-8 character set."
category: "html"
order: 24
---

Emojis are characters from the UTF-8 character set.

### What are Emojis?
Emojis look like images, or icons, but they are not. They are letters (characters) from the UTF-8 (Unicode) character set.

### The `<meta>` Charset Attribute
To display an HTML page correctly, a web browser must know the character set used in the page.
This is specified in the `<meta>` tag:
`<meta charset="UTF-8">`

### Emoji Characters
Since Emojis are characters, they can be copied and pasted just like any other letter in HTML.
Or, you can use the Decimal Entity number representing the emoji.

<Editor 
    initialHtml={`<h2>HTML Emojis</h2>\n<p>I will display some emojis here: &#128512; &#128516; &#128525; &#128151;</p>\n<p>You can also increase their size using CSS:</p>\n<p style="font-size:48px">&#128125;</p>\n<p style="font-size:100px">&#128187;</p>`}
/>
