---
title: "HTML Paragraphs"
description: "A paragraph always starts on a new line, and is usually a block of text."
category: "html"
order: 3
---

## HTML Paragraphs

The HTML `<p>` element defines a paragraph. Browsers automatically add margin space before and after each paragraph.

### Try It — Paragraphs, Line Breaks & Dividers

<Editor 
    initialHtml={`<h2>HTML Paragraphs</h2>\n\n<p>This is the first paragraph. A paragraph always starts on a new line.</p>\n\n<p>This is the second paragraph. Browsers automatically add some white space above and below each paragraph.</p>\n\n<hr>\n\n<h3>Using Line Breaks</h3>\n<p>\n  Line 1 of this paragraph.<br>\n  Line 2 — forced with a &lt;br&gt; tag.<br>\n  Line 3 — still the same paragraph!\n</p>\n\n<hr>\n\n<h3>Extra Spaces Are Ignored</h3>\n<p>Even if you add many   spaces   or\nline\nbreaks in the source code,\nthe browser just shows it as one continuous paragraph.</p>`}
    initialCss={`body {\n  font-family: Arial, sans-serif;\n  padding: 16px;\n  line-height: 1.7;\n  color: #333;\n}\nh2 { color: #04AA6D; }\nh3 { color: #555; margin-top: 16px; }\nhr { border: none; border-top: 2px solid #ddd; margin: 16px 0; }`}
/>

### Key Elements

| Tag | What it does |
| :--- | :--- |
| `<p>` | Defines a paragraph (block-level, adds spacing) |
| `<br>` | Line break — new line within the same paragraph |
| `<hr>` | Horizontal rule — a thematic divider line |

> **Tip:** Use `<br>` only when *line breaks are part of the content* (e.g. addresses or poetry). For general spacing between blocks, use separate `<p>` elements or CSS margin.
