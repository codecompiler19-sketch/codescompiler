---
title: "JS Statements"
description: "JavaScript statements are instructions executed by the browser one by one."
category: "javascript"
order: 3
---

A JavaScript **statement** is a single instruction for the browser to execute. A **program** is a series of statements executed top to bottom.

Statements are built from: **values**, **operators**, **expressions**, **keywords**, and **comments**.

### Try It — Statements in Practice

<Editor 
    initialHtml={`<h2>JavaScript Statements</h2>\n\n<div id="s1" class="result"></div>\n<div id="s2" class="result"></div>\n<div id="s3" class="result"></div>\n<div id="s4" class="result"></div>`}
    initialCss={`body { font-family: Arial, sans-serif; padding: 16px; }\nh2 { color: #04AA6D; }\n.result {\n  background: #f4f4f4;\n  border-left: 4px solid #04AA6D;\n  padding: 10px 14px;\n  font-family: monospace;\n  margin: 8px 0;\n  font-size: 14px;\n}`}
    initialJs={`// Statement 1 — assign and compute\nlet a = 10;\nlet b = 25;\nlet sum = a + b;\ndocument.getElementById("s1").textContent =\n  "Statement 1: " + a + " + " + b + " = " + sum;\n\n// Statement 2 — multiple statements on one line (semicolon separates them)\nlet x = 3; let y = 7; let product = x * y;\ndocument.getElementById("s2").textContent =\n  "Statement 2 (one line): " + x + " × " + y + " = " + product;\n\n// Statement 3 — white space is ignored\nlet   city   =   "Delhi";\ndocument.getElementById("s3").textContent =\n  "Statement 3 (extra spaces ignored): city = " + city;\n\n// Statement 4 — a statement can span multiple lines\nlet message = "Statements can "\n            + "span multiple "\n            + "lines!";\ndocument.getElementById("s4").textContent = "Statement 4: " + message;`}
/>

### Statement Rules

| Rule | Example |
| :--- | :--- |
| End with semicolon `;` | `let x = 5;` |
| Runs top to bottom | First line → last line |
| White space ignored | `let x=5;` = `let x = 5;` |
| Multiple per line | `let a=1; let b=2;` |
| Can span lines | `let msg = "Hello" + " World";` |

> **Tip:** Always add semicolons at the end of statements. While JavaScript can sometimes guess where they go, omitting them can cause hard-to-find bugs.
