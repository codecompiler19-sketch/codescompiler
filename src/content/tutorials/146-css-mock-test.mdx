---
title: "CSS Mock Test"
description: "Learn CSS Mock Test with clear explanation, code examples, and practical guide."
category: "css"
order: 1460
group: "CSS References"
seoTitle: "CSS Mock Test — CodesCompiler CSS Guide"
permalink: "css-mock-test"
---


# CSS Mock Test


PreviousNext


This section presents you various set of Mock Tests related toCSS.  You can download these sample mock tests at your local machine and solve offline at your convenience. Every mock test is supplied with a mock test key to let you verify the final score and grade yourself.


Mock Test I


Mock Test II


Mock Test III


Mock Test IV

php $min = 1;
   $max = 25;
   if( isset( $_GET['min'] )) &#123;
       $min = $_GET['min'];
   &#125;
   if( isset( $_GET['max'] )) &#123;
       $max = $_GET['max'];
   &#125;
   $total = 100;
   if( !is_numeric( $min ) || !is_numeric( $max ) )&#123;
       $min = 1;
       $max = 40;
   &#125;
   if( $min `< `0 || $max
$total )&#123;
       $min = 1;
       $max = 40;
   &#125;
   if( $min == 1 )&#123;
       $N = "I";
       $test_file = "css_mock_test_i.pdf";
   &#125;else if  ($min == 26 )&#123;
       $N = "II";
       $test_file = "css_mock_test_ii.pdf";
   &#125;else if ( $min == 51 )&#123;
       $N = "III";
       $test_file = "css_mock_test_iii.pdf";
   &#125;else if ( $min == 76 )&#123;
       $N = "IV";
       $test_file = "css_mock_test_iv.pdf";
   &#125;
   echo "

## CSS Mock Test $N

\n";
   $QN = 0;
   $key = array();
   for($i = $min; $i (.*)(.*)/', $line, $matches) )&#123;
             array_push( $key, $matches[4] );
          &#125;
      &#125;
   &#125;
   include ("answer-key.php");
?>

Download


css_questions_answers.htm

