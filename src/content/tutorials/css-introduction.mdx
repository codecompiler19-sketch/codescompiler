---
title: "CSS Introduction"
description: "CSS (Cascading Style Sheets) is used to format the layout of a webpage and style HTML elements."
category: "css"
order: 1
group: "CSS Basics"
seoTitle: "CSS Introduction — Learn CSS Basics"
permalink: "css-introduction"
---

# CSS Introduction

CSS is the language we use to style an HTML document. CSS describes how HTML elements should be displayed.

## What is CSS?

- **CSS** stands for Cascading Style Sheets
- CSS describes how HTML elements are to be displayed on screen, paper, or in other media
- CSS saves a lot of work. It can control the layout of multiple web pages all at once
- External stylesheets are stored in CSS files

## Why Use CSS?

CSS is used to define styles for your web pages, including the design, layout, and variations in display for different devices and screen sizes.

```css
body {
  background-color: lightblue;
}

h1 {
  color: white;
  text-align: center;
}

p {
  font-family: verdana;
  font-size: 20px;
}
```

---

## Live Interactive CSS Editor

Try modifying the CSS styles below to see how the card transforms in real time!

<Editor 
    initialHtml={`<div class="card">
  <h2>CSS Styling Demo</h2>
  <p>CSS brings your web pages to life with vibrant colors, beautiful fonts, and responsive layouts.</p>
  <button class="btn">Learn More</button>
</div>`}
    initialCss={`body { 
  background: #f0f4f8; 
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
  padding: 30px; 
}

.card { 
  background: white; 
  padding: 28px; 
  border-radius: 12px; 
  box-shadow: 0 4px 15px rgba(0,0,0,0.08); 
  border-left: 6px solid #264DE4; 
  max-width: 450px;
  margin: 0 auto;
}

h2 { 
  color: #264DE4; 
  margin-top: 0; 
}

p {
  color: #4a5568;
  line-height: 1.6;
}

.btn {
  background: #264DE4;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 6px;
  cursor: pointer;
  font-weight: 600;
  transition: background 0.2s;
}

.btn:hover {
  background: #1a365d;
}`}
    initialJs={``}
/>
