---
title: "CSS Introduction"
description: "CSS is the language we use to style an HTML document."
category: "css"
order: 1
---

## What is CSS?

- **CSS** stands for **Cascading Style Sheets**
- CSS describes how HTML elements are to be displayed on screen, paper, or in other media
- CSS saves a lot of work. It can control the layout of multiple web pages all at once
- External stylesheets are stored in CSS files

### CSS Demo - Try It!

In this tutorial, you can experiment with CSS styles. Click on the CSS tab in the editor to modify the styles.

<Editor 
    initialHtml={`<h1>My First CSS Example</h1>\n<p>This is a paragraph.</p>`}
    initialCss={`body {\n  background-color: lightblue;\n}\n\nh1 {\n  color: white;\n  text-align: center;\n}\n\np {\n  font-family: verdana;\n  font-size: 20px;\n}`}
/>

### Why Use CSS?

CSS is used to define styles for your web pages, including the design, layout and variations in display for different devices and screen sizes.
