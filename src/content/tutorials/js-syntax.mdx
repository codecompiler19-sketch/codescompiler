---
title: "JS Syntax"
description: "JavaScript syntax is the set of rules defining how JavaScript programs are written."
category: "javascript"
order: 4
---

JavaScript syntax defines the rules for writing valid programs. The three core building blocks are **literals** (fixed values), **variables** (named storage), and **expressions** (computed values).

### Try It — Syntax Building Blocks

<Editor 
    initialHtml={`<h2>JavaScript Syntax</h2>\n\n<h3>Literals (fixed values)</h3>\n<div id="literals" class="result"></div>\n\n<h3>Variables (named storage)</h3>\n<div id="vars" class="result"></div>\n\n<h3>Expressions (computed values)</h3>\n<div id="expr" class="result"></div>\n\n<h3>Keywords in action</h3>\n<div id="keywords" class="result"></div>`}
    initialCss={`body { font-family: Arial, sans-serif; padding: 16px; }\nh2 { color: #04AA6D; }\nh3 { color: #555; margin: 16px 0 4px; }\n.result {\n  background: #f4f4f4;\n  border-left: 4px solid #04AA6D;\n  padding: 10px 14px;\n  font-family: monospace;\n  font-size: 14px;\n  margin: 0;\n}`}
    initialJs={`// LITERALS — fixed values written directly\nlet numLiteral = 42;         // number literal\nlet strLiteral = "Hello!";   // string literal\nlet boolLiteral = true;      // boolean literal\ndocument.getElementById("literals").textContent =\n  numLiteral + " | " + strLiteral + " | " + boolLiteral;\n\n// VARIABLES — containers for data (let, const, var)\nlet firstName = "Ravi";\nconst country = "India";   // const = cannot change\ndocument.getElementById("vars").textContent =\n  "Name: " + firstName + " | Country: " + country;\n\n// EXPRESSIONS — operations that produce a value\nlet width = 8;\nlet height = 5;\nlet area = width * height;          // arithmetic expression\nlet greeting = "Hello, " + firstName;  // string expression\ndocument.getElementById("expr").textContent =\n  "Area: " + area + " | Greeting: " + greeting;\n\n// KEYWORDS — reserved words with special meaning\nlet score = 72;\nlet grade;\nif (score >= 75) {\n  grade = "Distinction";\n} else if (score >= 60) {\n  grade = "Pass";\n} else {\n  grade = "Fail";\n}\ndocument.getElementById("keywords").textContent =\n  "Score: " + score + " → Grade: " + grade;`}
/>

### Syntax Quick Reference

| Concept | Example |
| :--- | :--- |
| Number literal | `42`, `3.14` |
| String literal | `"Hello"`, `'World'` |
| Boolean literal | `true`, `false` |
| `let` variable | `let name = "Alice";` |
| `const` constant | `const PI = 3.14;` |
| Expression | `width * height` |
| Comment | `// this is a comment` |

> **Tip:** Use `const` for values that won't change (most things). Use `let` when the value will be reassigned. Avoid `var` in modern JavaScript.
