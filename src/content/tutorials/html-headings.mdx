---
title: HTML Headings
description: |
    Learn how to use HTML headings h1 through h6
category: html
order: 2
date: 2026-04-13T21:40:42.024Z
fmContentType: Tutorial
lastmod: 2026-04-13T21:43:54.132Z
---
## HTML Headings

HTML headings are defined with the `<h1>` to `<h6>` tags.

`<h1>` defines the **most important** heading. `<h6>` defines the **least important** heading.

---

## Example

<Editor
  initialHtml={`<h1>Heading 1</h1>
<h2>Heading 2</h2>
<h3>Heading 3</h3>
<h4>Heading 4</h4>
<h5>Heading 5</h5>
<h6>Heading 6</h6>`}
  initialCss={`body {
  font-family: sans-serif;
  padding: 2rem;
}
h1 { color: #e44d26; }
h2 { color: #f16529; }
h3 { color: #264de4; }
h4 { color: #04AA6D; }
h5 { color: #9b59b6; }
h6 { color: #888; }`}
/>

---

## Headings Are Important

- Search engines use headings to **index** the structure of your page
- Users skim pages by their headings — use them to show the **document structure**
- Use `<h1>` headings for **main headings**, followed by `<h2>`, then `<h3>`, and so on

> **Tip:** Use HTML headings for headings only. Do **not** use headings to just make text Big or Bold.

---

## Bigger Headings

Each HTML heading has a **default size**. You can change the size using the `style` attribute:

<Editor
  initialHtml={`<h1 style="font-size:60px;">Heading 1</h1>
<p>This heading is 60 pixels big.</p>`}
/>
