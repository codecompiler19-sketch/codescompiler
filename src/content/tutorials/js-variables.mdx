---
title: "JS Variables"
description: "JavaScript variables are containers for storing data values."
category: "javascript"
order: 6
---

Variables are **named containers** that store values you can reuse throughout your program. JavaScript gives you three keywords to declare them.

| Keyword | Reassignable | Scope | Use When |
| :--- | :--- | :--- | :--- |
| `const` | ❌ No | Block | Value won't change *(recommended default)* |
| `let` | ✅ Yes | Block | Value will change |
| `var` | ✅ Yes | Function | Old code — avoid in modern JS |

### Try It — Variables in Action

<Editor 
    initialHtml={`<h2>JavaScript Variables</h2>\n\n<div id="r1" class="result"></div>\n<div id="r2" class="result"></div>\n<div id="r3" class="result"></div>\n<div id="r4" class="result"></div>\n<div id="r5" class="result"></div>`}
    initialCss={`body { font-family: Arial, sans-serif; padding: 16px; }\nh2 { color: #04AA6D; }\n.result {\n  background: #f4f4f4;\n  border-left: 4px solid #04AA6D;\n  padding: 10px 14px;\n  font-family: monospace;\n  font-size: 14px;\n  margin: 8px 0;\n}`}
    initialJs={`// const — value cannot be reassigned\nconst siteName = "CodesCompiler";\ndocument.getElementById("r1").textContent = "const siteName = " + siteName;\n\n// let — value CAN be reassigned\nlet score = 40;\nscore = 95;  // updated!\ndocument.getElementById("r2").textContent = "let score (updated): " + score;\n\n// Arithmetic with variables\nconst price1 = 49.99;\nconst price2 = 19.99;\nlet total = price1 + price2;\ndocument.getElementById("r3").textContent =\n  "Total: $" + price1 + " + $" + price2 + " = $" + total.toFixed(2);\n\n// Variables are case-sensitive\nlet colour = "Blue";\nlet Colour = "Red";\ndocument.getElementById("r4").textContent =\n  "colour = " + colour + " | Colour = " + Colour + " (different variables!)";\n\n// Undefined — declared but not yet assigned\nlet city;\ndocument.getElementById("r5").textContent =\n  "city before assignment: " + city + " (type: " + typeof city + ")";`}
/>

### Variable Naming Rules

- Must start with a letter, `$`, or `_`
- Can contain letters, digits, `$`, `_`
- **Case sensitive** — `name` ≠ `Name`
- Cannot be a reserved keyword (`let`, `if`, `return`, etc.)

> **Tip:** Good variable names describe what the value means: `userName`, `totalPrice`, `isLoggedIn`. Avoid single letters like `x` or vague names like `data`.
