---
title: "HTML Lists"
description: "HTML lists allow web developers to group a set of related items in lists."
category: "html"
order: 9
---

HTML provides three types of lists. Use them to present grouped, structured information clearly.

### Try It — All List Types

<Editor 
    initialHtml={`<h2>Types of HTML Lists</h2>\n\n<h3>Unordered List (ul)</h3>\n<ul>\n  <li>HTML</li>\n  <li>CSS</li>\n  <li>JavaScript</li>\n</ul>\n\n<h3>Ordered List (ol)</h3>\n<ol>\n  <li>Learn HTML first</li>\n  <li>Then learn CSS</li>\n  <li>Finally, learn JavaScript</li>\n</ol>\n\n<h3>Description List (dl)</h3>\n<dl>\n  <dt>HTML</dt>\n  <dd>The structure and content of a web page</dd>\n  <dt>CSS</dt>\n  <dd>The visual styling and layout</dd>\n  <dt>JavaScript</dt>\n  <dd>The interactivity and dynamic behaviour</dd>\n</dl>`}
    initialCss={`body {\n  font-family: Arial, sans-serif;\n  padding: 16px;\n  line-height: 1.8;\n}\nh2 { color: #04AA6D; }\nh3 { color: #333; margin-top: 20px; }\nul { list-style-type: disc; padding-left: 24px; }\nol { list-style-type: decimal; padding-left: 24px; }\ndl { background: #f4f4f4; padding: 12px 16px; border-radius: 6px; }\ndt { font-weight: bold; color: #04AA6D; }\ndd { margin: 0 0 8px 16px; color: #555; }`}
/>

### List Types Explained

| Tag | Type | Description |
| :--- | :--- | :--- |
| `<ul>` | Unordered | Bullet points — order doesn't matter |
| `<ol>` | Ordered | Numbered — sequence matters |
| `<dl>` | Description | Term + definition pairs |
| `<li>` | List Item | Used inside `<ul>` and `<ol>` |
| `<dt>` | Term | Used inside `<dl>` |
| `<dd>` | Description | Used inside `<dl>` to describe `<dt>` |

> **Tip:** You can customise the bullet style of `<ul>` lists using CSS: `list-style-type: circle`, `square`, or `none`.
