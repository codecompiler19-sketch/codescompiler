---
title: "PYTHON - Set Exercises"
description: "Learn PYTHON - Set Exercises with clear explanation, code examples, and practical guide."
category: "python"
order: 980
group: "Python Sets"
seoTitle: "PYTHON - Set Exercises — CodesCompiler PYTHON Guide"
permalink: "python-set-exercises"
---


# Python - Set Exercises




# Python Set Exercise 1


Python program to find common elements in two lists with the help of set operations −


```python
l1=[1,2,3,4,5]
l2=[4,5,6,7,8]
s1=set(l1)
s2=set(l2)
commons = s1&s2 # or s1.intersection(s2)
commonlist = list(commons)
print (commonlist)
```


It will produce the followingoutput−


```python
[4, 5]
```


## Python Set Exercise 2


Python program to check if a set is a subset of another −


```python
s1={1,2,3,4,5}
s2={4,5}
if s2.issubset(s1):
   print ("s2 is a subset of s1")
else:
   print ("s2 is not a subset of s1")
```


It will produce the followingoutput−


```python
s2 is a subset of s1
```


## Python Set Exercise 3


Python program to obtain a list of unique elements in a list −


```python
T1 = (1, 9, 1, 6, 3, 4, 5, 1, 1, 2, 5, 6, 7, 8, 9, 2)
s1 = set(T1)
print (s1)
```


It will produce the followingoutput−


```python
{1, 2, 3, 4, 5, 6, 7, 8, 9}
```

### Exercise Programs
`<ul class="list">`
`<li>`pPython program to find the size of a set object.`</li>`
`<li>`pPython program that splits a set into two based on odd/even numbers.`</li>`
`<li>`pPython program to remove all negative numbers from a set.`</li>`
`<li>`pPython program to build another set with absolute value of each number in a set.`</li>`
`<li>`pPython program to remove all strings from a set which has elements of different types.`</li>`
`</ul>`
