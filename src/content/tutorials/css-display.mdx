---
title: "CSS Display"
description: "The display property is the most important CSS property for controlling layout."
category: "css"
order: 9
---

The `display` property controls **how an element behaves in the page flow**. It's the most important property for layout.

Every HTML element has a default display value — most are either `block` or `inline`.

### Try It — Block vs Inline vs None

<Editor 
    initialHtml={`<h2>CSS Display Demo</h2>\n\n<h3>Block Elements (take full width)</h3>\n<div class="block-demo">I am a DIV — block by default</div>\n<p class="block-demo">I am a P — also block</p>\n\n<h3>Inline Elements (only as wide as content)</h3>\n<span class="inline-demo">Span 1</span>\n<span class="inline-demo">Span 2</span>\n<span class="inline-demo">Span 3</span>\n\n<h3>Making a block element inline (inline-block)</h3>\n<div class="iblock">Box A</div>\n<div class="iblock">Box B</div>\n<div class="iblock">Box C</div>\n\n<h3>visibility: hidden vs display: none</h3>\n<p class="vis-hidden">This is visibility:hidden — I take up space but I'm invisible!</p>\n<p class="dis-none">This is display:none — I am completely gone!</p>\n<p>The gap above shows where visibility:hidden sits. The line below is unbroken.</p>`}
    initialCss={`body { font-family: Arial, sans-serif; padding: 16px; }\nh2 { color: #04AA6D; }\nh3 { color: #555; margin: 18px 0 8px; }\n\n.block-demo {\n  display: block;\n  background: #d4edda;\n  border: 2px solid #28a745;\n  padding: 8px 12px;\n  color: #155724;\n  margin-bottom: 6px;\n}\n\n.inline-demo {\n  display: inline;\n  background: #d1ecf1;\n  border: 2px solid #0c5460;\n  padding: 4px 10px;\n  color: #0c5460;\n  margin: 2px;\n}\n\n.iblock {\n  display: inline-block;\n  background: #04AA6D;\n  color: white;\n  padding: 10px 20px;\n  margin: 4px;\n  border-radius: 5px;\n  font-weight: bold;\n}\n\n.vis-hidden {\n  visibility: hidden;\n  background: #f8d7da;\n  padding: 8px;\n  margin: 0;\n}\n\n.dis-none {\n  display: none; /* completely removed from layout */\n}`}
/>

### Display Values Reference

| Value | Behaviour |
| :--- | :--- |
| `block` | Starts on new line, takes full width |
| `inline` | Flows within text, only as wide as content |
| `inline-block` | Like inline but respects width/height/padding |
| `none` | Removed from page entirely (no space) |
| `flex` | Flexbox layout (modern, powerful) |
| `grid` | CSS Grid layout |

> **Tip:** `display: none` vs `visibility: hidden` — `none` removes the element from the layout (no space). `hidden` makes it invisible but **keeps the space**.
