---
title: "HTML Attributes"
description: "Attributes provide additional information about HTML elements."
category: "html"
order: 10
---

All HTML elements can have **attributes**. Attributes provide additional information about elements and are always specified in the start tag.

Attributes usually come in name/value pairs like: `name="value"`.

### Try It — The href Attribute
The `<a>` tag defines a hyperlink. The `href` attribute specifies the URL of the page the link goes to.

<Editor 
    initialHtml={`<h2>The href Attribute</h2>\n<p>HTML links are defined with the a tag. The link address is specified in the href attribute:</p>\n\n<a href="https://codescompiler.com">Visit CodesCompiler</a>`}
/>

### The src Attribute
The `<img>` tag is used to embed an image in an HTML page. The `src` attribute specifies the path to the image to be displayed.

<Editor 
    initialHtml={`<h2>The src Attribute</h2>\n<p>HTML images are defined with the img tag, and the filename of the image source is specified in the src attribute:</p>\n\n<img src="https://www.w3schools.com/images/w3schools_green.jpg" alt="Logo" width="104" height="142">`}
/>

### The style Attribute
The `style` attribute is used to add styles to an element, such as color, font, size, and more.

<Editor 
    initialHtml={`<h2>The style Attribute</h2>\n<p>The style attribute is used to specify the styling of an element, like color, font, size etc.</p>\n\n<p style="color:red">I am a red paragraph.</p>\n<p style="font-size:50px">I am a huge paragraph.</p>`}
/>

### Common HTML Attributes

*   `href` - Specifies the URL of the linked resource
*   `src` - Specifies the URL of an image resource
*   `style` - Specifies inline CSS styles for an element
*   `alt` - Specifies an alternative text for an image
*   `title` - Provides extra information about an element (displayed as a tooltip)
