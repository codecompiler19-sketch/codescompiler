---
title: "JS Data Types"
description: "JavaScript variables can hold different data types: strings, numbers, booleans, objects, arrays, and more."
category: "javascript"
order: 8
---

JavaScript is **dynamically typed** — a variable can hold any type of data, and the type can change at runtime. Use `typeof` to check any variable's type.

### Try It — All Data Types

<Editor 
    initialHtml={`<h2>JavaScript Data Types</h2>\n<div id="str"   class="result"></div>\n<div id="num"   class="result"></div>\n<div id="bool"  class="result"></div>\n<div id="undef" class="result"></div>\n<div id="null_" class="result"></div>\n<div id="obj"   class="result"></div>\n<div id="arr"   class="result"></div>\n<div id="dyn"   class="result"></div>`}
    initialCss={`body { font-family: Arial, sans-serif; padding: 16px; }\nh2 { color: #04AA6D; margin-bottom: 12px; }\n.result {\n  background: #f4f4f4;\n  border-left: 4px solid #04AA6D;\n  padding: 8px 14px;\n  font-family: monospace;\n  font-size: 13px;\n  margin: 5px 0;\n}`}
    initialJs={`// String — text in quotes\nlet name = "CodesCompiler";\ndocument.getElementById("str").textContent =\n  'String: "' + name + '"  →  typeof: ' + typeof name;\n\n// Number — integers and decimals\nlet price = 29.99;\ndocument.getElementById("num").textContent =\n  "Number: " + price + "  →  typeof: " + typeof price;\n\n// Boolean — true or false\nlet isActive = true;\ndocument.getElementById("bool").textContent =\n  "Boolean: " + isActive + "  →  typeof: " + typeof isActive;\n\n// Undefined — declared but not assigned\nlet city;\ndocument.getElementById("undef").textContent =\n  "Undefined: " + city + "  →  typeof: " + typeof city;\n\n// Null — intentionally empty\nlet user = null;\ndocument.getElementById("null_").textContent =\n  "Null: " + user + "  →  typeof: " + typeof user + " (quirk: JS says 'object')";\n\n// Object — key-value pairs\nlet person = { firstName: "Ravi", age: 25 };\ndocument.getElementById("obj").textContent =\n  "Object: name=" + person.firstName + ", age=" + person.age + "  →  typeof: " + typeof person;\n\n// Array — ordered list\nlet colors = ["red", "green", "blue"];\ndocument.getElementById("arr").textContent =\n  "Array: [" + colors.join(", ") + "]  →  length: " + colors.length;\n\n// Dynamic typing — same variable, different type\nlet x = 100;\ndocument.getElementById("dyn").textContent =\n  "Dynamic: x starts as " + typeof x + " (" + x + ")";\nx = "now a string!";\ndocument.getElementById("dyn").textContent +=\n  "  →  then becomes " + typeof x + ' ("' + x + '")';`}
/>

### Data Types at a Glance

| Type | Example | `typeof` result |
| :--- | :--- | :--- |
| String | `"Hello"` | `"string"` |
| Number | `42`, `3.14` | `"number"` |
| Boolean | `true`, `false` | `"boolean"` |
| Undefined | `let x;` | `"undefined"` |
| Null | `null` | `"object"` *(JS quirk)* |
| Object | `{ key: value }` | `"object"` |
| Array | `[1, 2, 3]` | `"object"` |

> **Tip:** Arrays are technically objects in JavaScript. Use `Array.isArray(value)` to reliably test if something is an array.
