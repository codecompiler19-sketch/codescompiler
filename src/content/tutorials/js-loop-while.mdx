---
title: "JS Loop While"
description: "The while loop loops through a block of code as long as a specified condition is true."
category: "javascript"
order: 25
---

Loops can execute a block of code as long as a specified condition is true.

### The While Loop
The `while` loop loops through a block of code as long as a specified condition is true.

```javascript
while (condition) {
  // code block to be executed
}
```

<Editor 
    initialHtml={`<h2>JavaScript While Loop</h2>\n<p id="demo"></p>`}
    initialJs={`let text = "";\nlet i = 0;\n\nwhile (i < 10) {\n  text += "<br>The number is " + i;\n  i++;\n}\n\ndocument.getElementById("demo").innerHTML = text;`}
/>

> **Note:** If you forget to increase the variable used in the condition, the loop will never end. This will crash your browser.

### The Do/While Loop
The `do...while` loop is a variant of the while loop. This loop will execute the code block once, before checking if the condition is true, then it will repeat the loop as long as the condition is true.

<Editor 
    initialHtml={`<h2>The Do/While Loop</h2>\n<p id="demo"></p>`}
    initialJs={`let text = "";\nlet i = 0;\n\ndo {\n  text += "<br>The number is " + i;\n  i++;\n}\nwhile (i < 5);\n\ndocument.getElementById("demo").innerHTML = text;`}
/>

### Comparing For and While
If you have read the previous chapter about the `for` loop, you will discover that a `while` loop is much like a `for` loop, with statement 1 and statement 3 omitted.
The loop in this example uses a `while` loop to collect the car names from the cars array:

<Editor 
    initialHtml={`<h2>While Loop with Arrays</h2>\n<p id="demo"></p>`}
    initialJs={`const cars = ["BMW", "Volvo", "Saab", "Ford"];\nlet i = 0;\nlet text = "";\n\nwhile (cars[i]) {\n  text += cars[i] + "<br>";\n  i++;\n}\n\ndocument.getElementById("demo").innerHTML = text;`}
/>
