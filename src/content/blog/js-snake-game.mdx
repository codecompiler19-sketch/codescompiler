---
title: "Neon Retro Snake Game"
description: "A neon retro reskin of the classic Snake game using HTML5 Canvas and WASD/Arrow key movements."
date: "2026-04-18"
category: "JavaScript Games"
tags: ["javascript", "canvas", "game", "retro"]
image: "/images/posts/js-snake-game.png"
featured: false
hasDemo: true
author: "CodesCompiler"
---

Retro games feel incredible on the canvas API. This snake array pushes new head coordinates and pops the tail unless it eats the apple.

## The Movement Update

```javascript
let snake = [{x: 10, y: 10}];
let velocity = {x: 1, y: 0};

function gameLoop() {
  const head = { x: snake[0].x + velocity.x, y: snake[0].y + velocity.y };
  snake.unshift(head);
  
  if (head.x === food.x && head.y === food.y) {
    score++;
    placeFood();
  } else {
    snake.pop(); // Remove tail if no food eaten
  }
}
```

See the Live Demo to experience the glowing neon aesthetics!
