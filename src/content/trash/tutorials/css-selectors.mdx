---
title: "CSS Selectors"
description: "CSS selectors are used to find or select the HTML elements you want to style."
category: "css"
order: 2
---

CSS selectors tell the browser **which** HTML elements to apply styles to. The three most essential selectors are the **element**, **id**, and **class** selectors.

### Try It — All Three Selectors

<Editor 
    initialHtml={`<h2>CSS Selectors Demo</h2>\n\n<!-- Element Selector: targets all <p> tags -->\n<p>I am a paragraph — styled by the element selector.</p>\n<p>Me too — all paragraphs are affected!</p>\n\n<!-- ID Selector: targets only this one element -->\n<p id="special">I have id="special" — unique style!</p>\n<p>I am a normal paragraph — not affected by #special.</p>\n\n<!-- Class Selector: targets any element with this class -->\n<p class="highlight">I have class="highlight" — styled!</p>\n<h3 class="highlight">This heading also has class="highlight"!</h3>\n<p>No class — I am unstyled.</p>`}
    initialCss={`body { font-family: Arial, sans-serif; padding: 16px; }\nh2 { color: #04AA6D; margin-bottom: 16px; }\nh3 { margin: 8px 0; }\n\n/* 1. Element Selector — targets ALL <p> elements */\np {\n  font-size: 15px;\n  color: #444;\n  margin: 6px 0;\n}\n\n/* 2. ID Selector — targets the ONE element with id="special" */\n#special {\n  background-color: #fff3cd;\n  border-left: 4px solid #f0ad4e;\n  padding: 8px 14px;\n  font-weight: bold;\n  color: #856404;\n  border-radius: 4px;\n}\n\n/* 3. Class Selector — targets ALL elements with class="highlight" */\n.highlight {\n  background-color: #d4edda;\n  border-left: 4px solid #28a745;\n  padding: 8px 14px;\n  color: #155724;\n  border-radius: 4px;\n}`}
/>

### Selector Reference

| Selector | Syntax | Targets |
| :--- | :--- | :--- |
| Element | `p { }` | All `<p>` elements on the page |
| ID | `#myId { }` | The **one** element with `id="myId"` |
| Class | `.myClass { }` | **All** elements with `class="myClass"` |
| Universal | `* { }` | Every single element on the page |
| Grouping | `h1, h2, p { }` | All h1, h2, and p elements together |

> **Tip:** IDs must be **unique** — one per page. Classes can be **reused** across many elements. Use classes for styles you want to apply to multiple things.
